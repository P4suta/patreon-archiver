[project]
name = "patreon-archiver"
version = "0.0.0"
description = "Docker-based local archiver for paid Patreon Cloudstream content"
requires-python = ">=3.13"
dependencies = [
    "yt-dlp[default]==2026.3.17",
    "beautifulsoup4>=4.13",
]

[tool.uv]
package = false
